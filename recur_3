

def number(num):
    if(num==0):
        return 
    if(num%2==0):
        #print(num) for even
        return number(num-1)
    else:
        print(num) # for odd
        return number(num-1)

num = float(input("Enter number that you want to print reverse:: "))
num = int(num)
number(num)
